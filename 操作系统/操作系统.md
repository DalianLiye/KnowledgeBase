# 进程
操作系统资源分配的最小单位（独立内存、文件句柄、网络端口等）\
例子：打开一个 JVM、Tomcat、浏览器、微信，每一个都是独立进程\
特点：进程间资源隔离，互不干扰，切换开销大

# 线程
进程内调度执行的最小单位（共享所属进程的内存 / 资源），也叫 “轻量级进程”\
例子：一个JVM进程里，会启动多条线程，包括：主线程、业务处理线程、GC 线程、定时任务线程等\
特点：同进程内线程共享资源，切换开销小，容易出现线程安全问题\