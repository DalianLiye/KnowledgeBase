[目录](../目录.md)


# 规约
规约(spec)表示对象的规约/规格\
规约描述了对象的期望状态 (Desired State)，即希望对象最终具备的特征、配置，以及对象的基本元信息（如名称、命名空间）\
规约是必需项，创建K8s对象时，必须由用户显式指定


# 状态
状态(status)表示对象当前的实际状态\
状态由K8s自身自动维护、更新，用户无法手动指定\
K8s通过一系列控制器持续监听、协调，不断让实际状态向期望状态逼近