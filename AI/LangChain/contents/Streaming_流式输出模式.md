[目录](../目录.md)


# 支持的流式输出模式

可以将以下一种或多种流式模式组成列表，传给 stream 或 astream 方法：

- **updates**\
  在每个agent步骤后，流式输出状态更新。如果在同一步骤中发生多次更新（例如运行了多个节点），这些更新会被分别流式输出。
- **messages**\
  从所有调用大模型的图节点中，流式输出 (token, metadata) 元组。
- **custom**\
  使用stream writer从你的图节点内部流式输出自定义数据。